var tinyMCEImageList = new Array(
  <% @page.page_files.each do |pf| %>
  ["<%= asset_url pf.file_file_name %>", "<%= asset_url pf.file.url(:normal, false) %>"],
  <% end %>
  ["Logo", "/images/logo.png"]
);
