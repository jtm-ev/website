
<div class='pages-subnav'>
  <h1><%= page.title %></h1>
  <ul class=''>
    <% page.children.in_navigation.each do |subpage| %>
      <li><%= navigation_link_to subpage.title, human_page_path(subpage.path) %></li>
    <% end %>
  </ul>
</div>
