
<div class='pages-subnav'>
  <h1><%= page.title %></h1>
  <ul>
    <% page.children.in_navigation.each do |subpage| %>
      <%= navigation_link_to subpage.title, human_page_path(subpage.path) %>
    <% end %>
  </ul>
</div>