<h1>Alle Projekte</h1>

<table>
  <tr>
    <th>Titel</th>
    <th>Beschreibung</th>
    <th>Jahr</th>
    <th>Genre</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @projects.each do |project| %>
  <tr>
    <td><%= project.title %></td>
    <td><%= project.description %></td>
    <td><%= project.year %></td>
    <td><%= project.genre %></td>
    <td><%= link_to 'Anzeigen', project %></td>
    <td><%= link_to 'Bearbeiten', edit_project_path(project) %></td>
    <td><%= link_to 'Löschen', project, method: :delete, data: { confirm: 'Bist du sicher?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'Neues Projekt', new_project_path %>
