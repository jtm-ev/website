<% set_background @project.images.first %>


<p id="notice"><%= notice %></p>

<h1><%= @project.title %></h1>

<% if @previous %>
  <%= link_to '<<<', show_tagged_projects_path(params[:tags], @previous) %>
<% else %>
  <<<
<% end %>
<%= link_to (params[:tags].try(:humanize) or 'Alle'), tagged_projects_path(params[:tags]) %>
<% if @next %>
  <%= link_to '>>>', show_tagged_projects_path(params[:tags], @next) %>
<% else %>
  >>>
<% end %>

<div class="slider-wrapper">
    <div id="slider" class="nivoSlider">
        <% @project.images.each do |item| %>
          <img src='<%= item.file.url(:large_cinema) %>' title='<%= item.description %>'>
        <% end %>
    </div>
</div>
<div id="htmlcaption" class="nivo-html-caption">
    <strong>This</strong> is an example of a <em>HTML</em> caption with <a href="#">a link</a>.
</div>

<ul class='tabs'>
  <li><a href='#description' data-toggle='tab'>Inhalt</a></li>
  <li><a href='#actors' data-toggle='tab'>Darsteller</a></li>
  <li><a href='#teams' data-toggle='tab'>Gruppen</a></li>
  <li><a href='#videos' data-toggle='tab'>Videos</a></li>
  <li><a href='#press' data-toggle='tab'>Presse (<%= @project.press.length %>)</a></li>
  <li><a href='#events' data-toggle='tab'>Termine (<%= @project.events.length %>)</a></li>
</ul>

<div class='tab-content'>
  <div id='description'>
    <%= @project.description.try :html_safe %>
  </div>

  <div id='events'>
    <ul>
      <% @project.events.each do |event| %>
        <li>
          <%= l event.start_time %>
          <% unless event.title.blank? %>
            (<%= event.title %>)
          <% end %>
        </li>
      <% end %>
    </ul>
  </div>

  <div id='press'>
    <% @project.press.each do |item| %>
      <a href='<%= item.file.url %>'>
        <img src='<%= item.file.url(:square) %>' title='<%= item.description %>'>
      </a>
    <% end %>
  </div>

  <div id='videos'>
    <% if @project.videos.nil? %>
      <p>Keine Videos verfügbar</p>
    <% else %>
      <% @project.videos.each_line do |video_url| %>
        <%= youtube_embed(video_url).html_safe %>
      <% end %>
    <% end %>
  </div> 

  <div id='actors'>
    <ul>
    
        <% @project.actor_team.team_memberships.each do |team_membership| %>
          <li>
            <%= link_to team_membership.member.full_name, team_membership.member %>
            <%= team_membership.role %>
          </li>
        <% end %>

    </ul>
  </div>

  <div id='teams'>
    <ul>
    <% @project.non_actor_teams.each do |team| %>
      <li><%= team.name %>
        <ul>
        <% team.team_memberships.each do |team_membership| %>
          <li>
            <%= link_to team_membership.member.full_name, team_membership.member %>
            <%= team_membership.role %>
          </li>
        <% end %>
        </ul>
      </li>
    <% end %>
    </ul>
  </div>
</div>

<% content_for :actions do %>
  <%= navigation_link_to 'Projekt bearbeiten', edit_project_path(@project) %>
<% end %>

