<div class='ui piled segment', style = "z-index: 4; margin: 0px; margin-bottom: 30px;">
  <h2 class="ui header">Passwort vergessen?</h2>
  <h3 class="ui header">Kein Problem!</h3>
  <p>
    Gib einfach unten deine registrierte E-Mail Adresse ein und wir senden dir einen Link mit dem du ein neues Passwort erstellen kannst.
  </p>

  <%= form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post }) do |f| %>

  <% unless devise_error_messages!.empty? %>
    <div class = "ui warning message">
      <%= devise_error_messages! %>
    </div>
  <% end %>

    <form class="ui large form">
      <div id="forgot_container" class="ui stacked segment">
        <div class="field">
          <div id="mail_forgot" class='ui left icon input'>
            <i class="mail outline icon"></i>
              <%= f.email_field :email, :autofocus => true, :placeholder => "E-Mail" %>
          </div>
        </div>
        <div>
          <%= f.submit "Neue Passwort anfordern", :class => "ui green button", :id => "submit_button_forgot" %>
        </div>
      </div>
    </form>
  <% end %>
</div>

<%= render "devise/shared/links" %>
